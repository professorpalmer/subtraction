# Session notes — 2026-08-01

## Intent

Stand up `subtraction` as a research repo on LLM addition bias vs pre-LLM subtractive craft. Kick Phase 0 with three Cursor-adapter models.

## Models (Cursor adapter)

- `gpt-5.6-luna-max` (requested as GPT Luna MAX)
- `grok-4.5`
- `composer-2.5`

## Open thoughts (operator)

Addition bias is probably **overdetermined**:

1. Task mix really did shift — feature velocity is the product.
2. But elite humans under the same "ship features" pressure still deleted more than agents do on cleanup/refactor prompts; that gap wants a non-task explanation (risk + preference + patch locality).
3. Token savings are **compounding**: every unnecessary line is re-read forever. Paying once for a subtract pass can win if the module stays hot.

Phase 0 should force the models to disagree productively on H-task vs H-risk vs H-arch.
